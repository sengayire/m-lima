import { ActiveLogin } from '../components';

export default {
  exact: true,
  name: 'Login to mlima marketplace',
  protected: false,
  path: '/activate_account/login',
  component: ActiveLogin,
};
