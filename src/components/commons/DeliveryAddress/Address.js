import React from 'react';

const Address = () => (
        <div className='order-description' style={{ display: 'flex', flexDirection: 'column' }}>
            <span>Full name</span>
            <span>Phone number</span>
            <span>Full address</span>
        </div>
);

export default Address;
